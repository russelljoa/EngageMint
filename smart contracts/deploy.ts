// Hardhat deploy script: deploys PlatformToken → Registry → Factory → Distributor, assigns roles, writes addresses.json.
// Fill in deployment logic as needed.

async function main() {
    // ...deployment steps...
}

main().catch((error) => {
    console.error(error);
    process.exitCode = 1;
});
