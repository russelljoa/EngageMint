// Hardhat config for Moonbeam, Moonbase Alpha, or EVM-compatible parachain.
// Fill in network settings as needed.

import { HardhatUserConfig } from "hardhat/config";

const config: HardhatUserConfig = {
    // ...networks, solidity version, etc...
};

export default config;
