// Hardhat verify script: runs hardhat run --verify for all contracts.
// Fill in verification logic as needed.

async function main() {
    // ...verification steps...
}

main().catch((error) => {
    console.error(error);
    process.exitCode = 1;
});
